################################################################
#
# 	Makefile for FileIO test
#
################################################################

################################################################
#	A rule to make sub directories.
################################################################

TOP_DIR 	= ../../..

FILEIO_DIR	= FILoad

all:
	@for dir in $(FILEIO_DIR); \
	do echo "---------------------------------------------"; \
	   echo "             make $$dir    			      "; \
	   echo "---------------------------------------------"; \
	$(MAKE) -C $$dir all || exit; \
	done


################################################################
#	Default rules
################################################################

clean:
	@for dir in $(FILEIO_DIR); \
	do $(MAKE) -C $$dir  clean || exit; \
	done
	
realclean:
	@for dir in $(FILEIO_DIR); \
	do $(MAKE) -C $$dir  realclean || exit; \
	done 


################################################################
# 	THE END
################################################################
